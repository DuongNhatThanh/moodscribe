import type { MoodResult } from '@/lib/types'

// analyzeEntry — calls Anthropic SDK and returns a MoodResult. Implemented in Phase 2.
export async function analyzeEntry(_content: string): Promise<MoodResult> {
  throw new Error('Not implemented yet')
}
